services:
  test:
    image: golang:1.24
    links:
      - database
    volumes:
      - ".:/src"
    environment:
      TEST_DB: "postgres://test:test@database/test?sslmode=disable"
    working_dir: "/src"
    command: "go test ./..."
